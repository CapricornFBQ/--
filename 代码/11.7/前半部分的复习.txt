1.数组

数组：是连续存储数据单位的集合。（小火车），数组中的数据可以通过下标访问，从0开始
数组的长度：arrays.length
数组的遍历：
         for(var i =0;i<arrays.length;i++){

		console.log(arrays[i]);
	}

数组的插入和删除
     pop删除   push插入
     shift从前删   unshift从前边增加
     splice 截取、删除、插入、替换
     join拼接数组元素，成为字符串    join("-"); 135-99-22-11
     split拆分



2.函数
  基本结构：定义

  function 函数名（参数，参数，参数）{

	代码块
        return 返回值；


 }

 
3.全局变量和局部变量
  全局变量：可以被全局和函数访问
  局部变量：在函数中定义，外界无法访问，生命周期和函数一致

